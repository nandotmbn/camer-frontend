import React from 'react';
import Monitoring from '../../views/Home/Monitoring';

function MonitoringView({navigation, route}) {
  return <Monitoring navigation={navigation} route={route} />;
}

export default MonitoringView;