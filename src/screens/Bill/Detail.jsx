import React from 'react';
import BillDetail from '../../views/Bill/Detail';

function BillDetailView({navigation, route}) {
  return <BillDetail navigation={navigation} route={route} />;
}

export default BillDetailView;
